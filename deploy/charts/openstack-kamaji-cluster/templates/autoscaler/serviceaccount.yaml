apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "openstack-kamaji-cluster.componentName" (list . "autoscaler") }}
  labels: {{ include "openstack-kamaji-cluster.componentLabels" (list . "autoscaler") | nindent 4 }}
